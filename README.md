# AspScript для VS Code

🚀 Официальное расширение Visual Studio Code для **AspScript** - революционного реактивного фреймворка без рантайма.

**Русская версия** | [English](./README.en.md)

## ✨ Возможности

### 🎨 Подсветка синтаксиса
- Полная подсветка синтаксиса для файлов `.aspc`
- Поддержка секции скриптов с разделителями `---`
- Подсветка HTML-шаблонов с директивами AspScript
- Подсветка секций стилей CSS/SCSS
- Реактивные ключевые слова: `$state`, `$computed`, `$effect`, `$global`
- Директивы: `@click`, `#bind`, `:class`, `#if`, `#for`

### ✅ Линтинг и диагностика
- Обнаружение ошибок в реальном времени
- Предупреждения о дублирующихся переменных
- Обнаружение неверных директив
- Проверка незакрытых фигурных скобок
- Предупреждения о пустых блоках
- Валидация структуры компонента

### 📝 Сниппеты
Быстрые сниппеты для частых паттернов:
- `aspc` - Полный шаблон компонента
- `state` - Реактивная переменная состояния
- `computed` - Вычисляемое значение
- `effect` - Побочный эффект
- `onMount` - Хук жизненного цикла
- `btn` - Кнопка с обработчиком клика
- `input` - Поле ввода с двусторонней привязкой
- `counter` - Полный компонент счетчика
- `form` - Полный компонент формы
- И многое другое!

### 🔧 Команды
- **AspScript: Create Component** - Создать новый компонент AspScript
- **AspScript: Compile Current File** - Скомпилировать активный `.aspc` файл
- **AspScript: Preview Component** - Предпросмотр компонента в боковой панели
- **AspScript: Format Document** - Форматировать код AspScript

### 💡 IntelliSense
- Автодополнение для реактивных ключевых слов
- Автодополнение для хуков жизненного цикла
- Автодополнение для директив
- Расширенная информация при наведении с примерами и синтаксисом
- Переход к определению для переменных и функций
- Символы документа (панель Outline)
- Поиск всех ссылок (скоро)

### 🔧 Действия с кодом
- Быстрые исправления частых ошибок
- Автоматическое добавление недостающих ключевых слов
- Удаление пустых блоков
- Форматирование при сохранении

### 🎯 Языковые функции
- Автозакрытие скобок и кавычек
- Автоматические отступы
- Сворачивание кода
- Переключение комментариев
- Умное сопоставление скобок

## 📦 Установка

### Из VS Code Marketplace
1. Откройте VS Code
2. Нажмите `Ctrl+Shift+X` (или `Cmd+Shift+X` на macOS)
3. Найдите "AspScript"
4. Нажмите "Установить"

### Из VSIX файла
1. Скачайте `.vsix` файл
2. Откройте VS Code
3. Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на macOS)
4. Выполните "Extensions: Install from VSIX..."
5. Выберите скачанный `.vsix` файл

## 🚀 Быстрый старт

1. Создайте новый файл с расширением `.aspc`
2. Напишите `aspc` и нажмите Tab для вставки шаблона компонента
3. Начните кодить с полной подсветкой синтаксиса и IntelliSense!

### Пример компонента

```aspscript
---
// Компонент счетчика
let count = $state(0)
$: doubled = count * 2

function increment() {
  count++
}
---

<div class="counter">
  <h2>Счёт: {count}</h2>
  <p>Удвоенное: {doubled}</p>
  <button @click="increment">Увеличить</button>
</div>

<style>
.counter {
  padding: 2rem;
  text-align: center;
}
</style>
```

## ⚙️ Настройка

Настройте расширение в параметрах VS Code:

### AspScript: Compiler Path
Путь к исполняемому файлу компилятора AspScript.
```json
{
  "aspscript.compilerPath": "aspc"
}
```

### AspScript: Auto Compile
Автоматически компилировать `.aspc` файлы при сохранении.
```json
{
  "aspscript.autoCompile": false
}
```

### AspScript: Format Enable
Включить/выключить форматирование кода.
```json
{
  "aspscript.format.enable": true
}
```

### AspScript: Format Indent Size
Количество пробелов для отступа.
```json
{
  "aspscript.format.indentSize": 2
}
```

### AspScript: Lint Enable
Включить/выключить линтинг.
```json
{
  "aspscript.lint.enable": true
}
```

### AspScript: Preview Auto Refresh
Автоматически обновлять предпросмотр при сохранении файла.
```json
{
  "aspscript.preview.autoRefresh": true
}
```

## 🎨 Справка по сниппетам

| Префикс | Описание |
|---------|----------|
| `aspc` | Полный шаблон компонента |
| `state` | Реактивная переменная состояния |
| `computed` | Вычисляемое значение |
| `effect` | Побочный эффект |
| `global` | Глобальное состояние |
| `onMount` | Хук OnMount |
| `onDestroy` | Хук OnDestroy |
| `func` | Функция |
| `afunc` | Асинхронная функция |
| `template` | Шаблон с интерполяцией |
| `btn` | Кнопка с обработчиком клика |
| `input` | Поле ввода с двусторонней привязкой |
| `if` | Условный элемент |
| `for` | Цикл |
| `class` | Динамическая привязка класса |
| `style` | Блок стилей |
| `scss` | Блок стилей SCSS |
| `counter` | Полный компонент счетчика |
| `form` | Полный компонент формы |

## 🎯 Горячие клавиши

- `Ctrl+Shift+C` - Скомпилировать текущий файл
- `Ctrl+Shift+P` - Предпросмотр компонента
- `Alt+Shift+F` - Форматировать документ

## 📚 Ресурсы

- [Документация AspScript](https://github.com/aspscript/aspscript)
- [Примеры](https://github.com/aspscript/aspscript/tree/main/examples)
- [Трекер проблем](https://github.com/aspscript/vscode-extension/issues)

## 🤝 Участие в разработке

Мы приветствуем вклад в проект! Пожалуйста, прочитайте наше [Руководство по разработке](DEVELOPMENT.md) для деталей.

## 📝 Лицензия

MIT © AspScript Team

## 🙏 Благодарности

- Вдохновлено Vue, Svelte и Solid.js
- Создано с ❤️ для сообщества разработчиков

---

**Приятного кодирования с AspScript!** 🚀
